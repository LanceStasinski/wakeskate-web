<GeneralContainer @load={{this.load}}>
  {{#if this.isShowingMap}}
    <Modal @onClick={{this.toggleModal}}>
      <Map @close={{this.close}} />
    </Modal>
  {{/if}}
  {{#if this.showData}}
    <Weather />
  {{/if}}
</GeneralContainer>