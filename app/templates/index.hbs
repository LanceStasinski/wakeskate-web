<GeneralContainer>
  {{#if this.isShowingMap}}
    <Modal @onClick={{this.toggleModal}}>
      <Map @close={{this.close}} />
    </Modal>
  {{/if}}
  {{#unless this.isShowingMap}}
    <Weather />
  {{/unless}}
</GeneralContainer>